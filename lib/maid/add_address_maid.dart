import 'package:flutter/material.dart';

class AddAddressMaid extends StatelessWidget {
  const AddAddressMaid({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(
        children: [

        ],
      ),
    );
  }
}
